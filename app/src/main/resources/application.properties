# App name (optional but useful for logging)
spring.application.name=back-end

# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://172.21.125.206:3306/cms?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=ACpXCAOleoTtCyvMVllkOgm6
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate (JPA) Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# MongoDB Configuration (replace with actual credentials if needed)
spring.data.mongodb.uri=mongodb://root:<mongodb_password>@<mongodb_host>:27017/prescriptions?authSource=admin

# Static Resources (e.g., JS/CSS/Images)
spring.web.resources.static-locations=classpath:/static/

# Thymeleaf Template Engine Settings
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML
spring.thymeleaf.cache=false           # Development mode: Live reload of templates
spring.thymeleaf.encoding=UTF-8        # Internationalization-friendly

# JWT Secret for token validation
jwt.secret=$!@#$^%$$$%####$DDCPN0234FCFDPD8670M

# API base path (custom usage depending on how you route endpoints)
api.path=/

# Spring Actuator Health Checks (useful in production monitoring)
management.endpoint.health.show-details=always
management.health.db.enabled=true
